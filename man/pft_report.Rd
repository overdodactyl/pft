% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{pft_report}
\alias{pft_report}
\title{Render a clinical PFT report}
\usage{
pft_report(result, output_file = NULL, ...)
}
\arguments{
\item{result}{A data frame produced by \code{\link[=pft_interpret]{pft_interpret()}} (or any of
the reference functions called with measured-value columns).}

\item{output_file}{Path to write the rendered report to. Defaults to a
tempfile in the session temp directory.}

\item{...}{Additional arguments passed to \code{\link[rmarkdown:render]{rmarkdown::render()}} (e.g.
\code{output_format}, \code{output_options}, \code{quiet}).}
}
\value{
The (invisible) path to the rendered report.
}
\description{
Renders a self-contained HTML clinical report summarising a patient's
(or, for \code{nrow > 1}, a cohort's first patient's) PFT result. The
report includes a demographic header, a per-measure table of predicted
values / measured values / z-scores / severity grades, the ATS
interpretive pattern, PRISm and bronchodilator-response status when
available, and the \code{pft_plot()} z-score lollipop figure.
}
\details{
Requires \code{rmarkdown} and \code{knitr}. The output is HTML by default;
callers can pass \code{output_format = "pdf_document"} if they have a
LaTeX install.
}
\examples{
\dontrun{
patient <- data.frame(
  sex = "M", age = 45, height = 178, race = "Caucasian",
  fev1_measured = 2.5, fvc_measured = 3.8,
  fev1fvc_measured = 2.5 / 3.8, tlc_measured = 6.0
)
result <- pft_interpret(patient)
pft_report(result, output_file = "patient_report.html")
}

}
\seealso{
\code{\link[=pft_interpret]{pft_interpret()}} to produce the result object this
function renders; \code{\link[=pft_plot]{pft_plot()}} for the embedded z-score figure.
}
