% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diffusion_interpret.R
\name{pft_diffusion_interpret}
\alias{pft_diffusion_interpret}
\title{Classify a diffusion result into a clinical pattern category}
\usage{
pft_diffusion_interpret(
  data,
  SI.units = FALSE,
  dlco = NULL,
  va = NULL,
  kco = NULL
)
}
\arguments{
\item{data}{A data frame containing the three z-score input columns.}

\item{SI.units}{Logical, default \code{FALSE}. Selects the default
column names for \code{dlco} and \code{kco}. Traditional units (\code{FALSE}):
\code{dlco_zscore} and \code{kco_tr_zscore}. SI units (\code{TRUE}): \code{tlco_zscore}
and \code{kco_si_zscore}. \code{va} defaults to \code{va_zscore} in both unit
systems.}

\item{dlco, va, kco}{Column references for the three z-score inputs.
\code{dlco} and \code{kco} default to \code{NULL}, which means: pick the
canonical column name based on \code{SI.units}. Pass a bare name, a
string, or \code{!!var} to override (see "Column-name overrides").}
}
\value{
The original data frame with a single appended column:
\code{diffusion_category}. Possible values:

\describe{
\item{\code{"Normal"}}{All three z-scores above LLN.}
\item{\code{"Parenchymal"}}{Low DLCO, low KCO, normal VA.}
\item{\code{"Volume loss"}}{Low DLCO, low VA, normal or elevated KCO.}
\item{\code{"Mixed"}}{Low DLCO, low VA, low KCO.}
\item{\code{"Vascular (suggested)"}}{Low DLCO, normal VA, low or
elevated KCO.}
\item{\code{"Elevated KCO"}}{Normal DLCO with elevated KCO (z >
+1.645).}
\item{\code{"Other"}}{Combination not matching any of the above
patterns (e.g., low VA in isolation).}
\item{\code{NA}}{Required z-score columns missing.}
}

The category labels describe the z-score pattern only; differential
diagnosis is left to the clinician (see the Hughes & Pride 2012
source paper for clinical interpretation).
}
\description{
Takes per-patient \code{dlco_zscore}, \code{va_zscore}, and \code{kco_zscore}
columns (the outputs of \code{\link[=pft_diffusion]{pft_diffusion()}} when \verb{_measured} columns
are supplied) and assigns a clinical interpretive category per the
Hughes & Pride 2012 framework adopted by the ERS/ATS Stanojevic
2017 task force.
}
\details{
The classifier consumes z-scores only and is unit-agnostic, but the
default input column names differ between unit systems. Set
\code{SI.units = TRUE} to pick up the SI-units column set
(\code{tlco_zscore}, \code{va_zscore}, \code{kco_si_zscore}); otherwise the
traditional-units column set (\code{dlco_zscore}, \code{va_zscore},
\code{kco_tr_zscore}) is used. Override individual column names via the
\code{dlco} / \code{va} / \code{kco} arguments.

Typically called via \code{\link[=pft_interpret]{pft_interpret()}} as part of the one-call
workflow; exported for callers who want to apply the classifier to
pre-computed z-score columns directly.
}
\section{Column-name overrides}{

Each column-reference argument accepts three forms:
\itemize{
\item a \strong{bare column name} -- \code{dlco = my_dlco}
\item a \strong{string} -- \code{dlco = "my_dlco"}
\item an \strong{injected value} -- \code{dlco = !!my_var} where \code{my_var <- "my_dlco"}
}

\code{dlco} and \code{kco} default to \code{NULL}, which selects the canonical
name based on \code{SI.units} (traditional or SI). Passing an explicit
reference overrides this selection.
}

\section{References}{

Hughes JM, Pride NB. Examination of the carbon monoxide diffusing
capacity (DL(CO)) in relation to its KCO and VA components.
\emph{Am J Respir Crit Care Med}. 2012;186(2):132-139.
\doi{10.1164/rccm.201112-2160CI}.

Stanojevic S, Graham BL, Cooper BG, et al. ERS/ATS technical
standard: Global Lung Function Initiative reference values for the
carbon monoxide transfer factor for Caucasians. \emph{Eur Respir
J}. 2017;50:1700010. \doi{10.1183/13993003.00010-2017}.
(Provides the z-score reference standard whose LLN at z = -1.645
is used here. The clinical interpretation framework is from
Hughes & Pride 2012, adopted by the 2017 task force.)
}

\examples{
# Three patients: normal, parenchymal (low DLCO/KCO, normal VA),
# and volume loss (low DLCO/VA, normal KCO).
d <- data.frame(
  dlco_zscore  = c(-0.5, -2.0, -2.0),
  va_zscore    = c(-0.5, -0.5, -2.0),
  kco_tr_zscore = c(-0.5, -2.0, -0.5)
)
pft_diffusion_interpret(d)

# SI units (TLCO / KCO_SI). Pass SI.units = TRUE.
d_si <- data.frame(
  tlco_zscore   = c(-0.5, -2.0),
  va_zscore     = c(-0.5, -0.5),
  kco_si_zscore = c(-0.5, -2.0)
)
pft_diffusion_interpret(d_si, SI.units = TRUE)

}
\seealso{
\code{\link[=pft_diffusion]{pft_diffusion()}} to compute the input z-scores;
\code{\link[=pft_interpret]{pft_interpret()}} for the one-call workflow that auto-runs this
classifier when diffusion outputs are present.
}
