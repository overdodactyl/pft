% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{pft_schema}
\alias{pft_schema}
\title{Describe the output column schema of \code{pft_interpret()} and friends}
\usage{
pft_schema(year = 2012, SI.units = FALSE, standard = c("2022", "2005"))
}
\arguments{
\item{year}{GLI spirometry equation year. \code{2012} or \code{2022}.}

\item{SI.units}{Whether diffusion outputs use SI units (TRUE → \code{tlco}
/ \code{kco_si} / \code{va}) or traditional (FALSE → \code{dlco} / \code{kco_tr} /
\code{va}).}

\item{standard}{Interpretive standard whose downstream severity /
pattern / BDR columns to enumerate. \code{"2022"} (Stanojevic) or
\code{"2005"} (Pellegrino).}
}
\value{
A tibble with columns:
\itemize{
\item \code{column}: the output column name as produced by the pipeline.
\item \code{measure}: the underlying measure key (e.g. \code{"fev1"}, \code{"tlc"},
\code{"dlco"}), or \code{NA} for whole-patient interpretation columns
(\code{ats_classification}, \code{prism}, \code{volume_subpattern}).
\item \code{statistic}: one of \code{"pred"}, \code{"lln"}, \code{"uln"}, \code{"zscore"},
\code{"pctpred"}, \code{"severity"}, \code{"classification"},
\code{"pattern_combination"}, \code{"prism"}, \code{"subpattern"},
\code{"bdr_pct"}, \code{"bdr_significant"}, \code{"bdr_abs"}.
\item \code{equation}: the reference standard or interpretive source.
\item \code{units}: human-readable units string.
\item \code{requires_measured}: \code{TRUE} if the column appears only when a
corresponding \verb{<measure>_measured} input was supplied.
\item \code{requires_pre_post}: \code{TRUE} if the column appears only when
\verb{<measure>_pre} and \verb{<measure>_post} inputs were supplied.
}
}
\description{
Returns a tibble enumerating every column that
\code{\link[=pft_interpret]{pft_interpret()}}, \code{\link[=pft_spirometry]{pft_spirometry()}}, \code{\link[=pft_volumes]{pft_volumes()}},
\code{\link[=pft_diffusion]{pft_diffusion()}}, and the downstream interpretation primitives can
emit, for a given combination of GLI spirometry year, diffusion unit
system, and interpretive standard. This is the symmetric counterpart
to \code{\link[=pft_required_columns]{pft_required_columns()}} (which documents \emph{inputs}) and is the
canonical surface for downstream consumers — Shiny apps, EMR
integrations, \code{tidymodels} recipes, and the \code{pft_to_fhir()} LOINC
adapter — that need to know "what columns will I get back?" without
running a sample interpretation and grepping the result.
}
\details{
Each row carries the column name, the measure key (e.g. \code{"fev1"}),
the statistic kind (e.g. \code{"pred"}), the equation source, the units,
and two booleans flagging whether the column appears only when a
specific input is also supplied (\code{requires_measured} for z-score /
percent-predicted / severity; \code{requires_pre_post} for
bronchodilator-response columns).
}
\section{Output-column naming caveat}{

GLI 2012 / 2021 / 2017 outputs use unsuffixed column names
(\code{fev1_pred}, \code{tlc_pred}, \code{dlco_pred}). GLI 2022 spirometry outputs
carry a \verb{_2022} suffix (\code{fev1_pred_2022}) so that callers can run
both standards on the same cohort. Volumes (GLI 2021) and diffusion
(GLI 2017) currently have no symmetric suffix; should a future GLI
revision land for either, a corresponding suffix will be added
consistently. This function is the authoritative source of truth
for the column-naming scheme on a given pipeline configuration.
}

\examples{
# Default: GLI 2012 spirometry, traditional diffusion, Stanojevic 2022.
pft_schema()

# GLI Global 2022 spirometry with SI diffusion.
pft_schema(year = 2022, SI.units = TRUE)

# All reference-value columns (pred / lln / uln only).
subset(pft_schema(), statistic \%in\% c("pred", "lln", "uln"))

}
\seealso{
\code{\link[=pft_required_columns]{pft_required_columns()}} for the input contract;
\code{\link[=pft_interpret]{pft_interpret()}} for the pipeline that produces these columns;
\code{\link[=pft_long]{pft_long()}} for a long-form pivot of the wide result.
}
