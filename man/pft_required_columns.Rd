% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/columns.R
\name{pft_required_columns}
\alias{pft_required_columns}
\title{Canonical input-column names for the \code{pft} reference functions}
\usage{
pft_required_columns(
  fun = c("pft_spirometry", "pft_volumes", "pft_diffusion", "pft_interpret"),
  year = 2012,
  SI.units = FALSE
)
}
\arguments{
\item{fun}{Name of the reference function to introspect. One of
\code{"pft_spirometry"}, \code{"pft_volumes"}, \code{"pft_diffusion"},
\code{"pft_interpret"}.}

\item{year}{Spirometry equation year (\code{2012} or \code{2022}). Only
relevant for \code{"pft_spirometry"} and \code{"pft_interpret"}: \code{race} is
required for 2012 but ignored for 2022.}

\item{SI.units}{Whether \code{\link[=pft_diffusion]{pft_diffusion()}} is configured to emit SI
units. Affects which \verb{<measure>_measured} columns are recognised
(\code{tlco_measured} / \code{kco_si_measured} for SI;
\code{dlco_measured} / \code{kco_tr_measured} for traditional).}
}
\value{
A named list with character-vector elements \code{required},
\code{optional_measured}, and (for \code{pft_interpret} only) \code{optional_bdr}.
}
\description{
\code{pft_required_columns()} returns the canonical column names that
\code{\link[=pft_spirometry]{pft_spirometry()}}, \code{\link[=pft_volumes]{pft_volumes()}}, \code{\link[=pft_diffusion]{pft_diffusion()}}, and
\code{\link[=pft_interpret]{pft_interpret()}} consume. Use it to introspect the input contract
before calling the reference functions, or to wire pft into a
validation step inside a larger pipeline.

Three groups of columns are reported per function:
\itemize{
\item \code{required} -- columns that \strong{must} be present. Missing required
columns raise an error.
\item \code{optional_measured} -- columns whose presence unlocks z-score and
percent-predicted outputs for the corresponding measure. Missing
these is silent; the function simply emits fewer output columns.
\item \code{optional_bdr} -- (\code{pft_interpret} only) \verb{<measure>_pre} /
\verb{<measure>_post} columns that, when present, unlock bronchodilator
response calculations.
}
}
\examples{
pft_required_columns("pft_spirometry", year = 2012)
pft_required_columns("pft_spirometry", year = 2022)
pft_required_columns("pft_volumes")
pft_required_columns("pft_diffusion", SI.units = TRUE)
pft_required_columns("pft_interpret", year = 2012)

}
\seealso{
\code{\link[=pft_spirometry]{pft_spirometry()}}, \code{\link[=pft_volumes]{pft_volumes()}}, \code{\link[=pft_diffusion]{pft_diffusion()}},
\code{\link[=pft_interpret]{pft_interpret()}}, \code{\link[=pft_validate]{pft_validate()}}.
}
