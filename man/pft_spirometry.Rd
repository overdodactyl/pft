% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spirometry.R
\name{pft_spirometry}
\alias{pft_spirometry}
\title{Compute spirometry reference values for given demographics}
\usage{
pft_spirometry(
  data,
  year = 2022,
  sex = sex,
  age = age,
  height = height,
  race = race
)
}
\arguments{
\item{data}{A data frame containing columns for sex ("M","F"),
race ("AfrAm","NEAsia","SEAsia","Other/mixed", "Caucasian"),
age (in years, in the range 3-95 for FEV1 / FVC / FEV1/FVC and
3-90 for FEF25-75 / FEF75 per the GLI spline tables), and
height (in centimeters).
Rows with \code{NA} in sex, age, or height (or, for GLI 2012, in race)
are returned with \code{NA} reference values. Race is ignored when
year = 2022 (GLI Global equations are race-neutral).

If \code{data} also contains any of \code{fev1_measured}, \code{fvc_measured},
\code{fev1fvc_measured}, \code{fef2575_measured}, \code{fef75_measured}, the
corresponding measured value is used to compute a z-score and
percent-predicted for that measure (see Value).}

\item{year}{The year of GLI published equations. Valid options are
2012 (multi-ethnic, requires a \code{race} column) and 2022 (race-neutral
"GLI Global"; the \code{race} column, if present, is ignored). Defaults
to \code{2022}, the current ERS/ATS recommendation.}

\item{sex, age, height, race}{Column references. By default
\code{pft_spirometry()} reads from \code{sex}, \code{age}, \code{height}, and (for GLI
2012) \code{race}. If your data frame names them differently, override
via a bare name (\code{sex = Sex}), a string (\code{sex = "Sex"}), or an
rlang injection (\code{sex = !!my_var}). The user's original column
names are preserved in the output.}
}
\value{
The original data frame with extra columns appended for each
measure. Every output column carries the GLI year as a suffix so
a single result frame can hold multiple equation outputs
side-by-side (\code{fev1_pred_2012}, \code{fev1_pred_2022}, ...).
\itemize{
\item \verb{<measure>_pred_<year>}: predicted (median) value.
\item \verb{<measure>_lln_<year>}:  lower limit of normal (5th percentile).
\item \verb{<measure>_uln_<year>}:  upper limit of normal (95th percentile).
If a \verb{<measure>_measured} column was supplied in \code{data}, two
additional columns are emitted:
\item \verb{<measure>_zscore_<year>}:  LMS z-score \code{((measured/M)^L - 1) / (L*S)}.
\item \verb{<measure>_pctpred_<year>}: percent predicted \code{(measured / pred) * 100}.
}
}
\description{
\code{pft_spirometry()} computes  ATS-compliant upper and lower normal limits
for common spirometry measures including FEV1, FVC, FEV1/FVC, FEF2575, and FEF75.
}
\examples{
data <- data.frame(sex=c("M","F"),
                   age=c(30.1,5.1),
                   height=c(178,50),
                   race=c("SEAsia","NEAsia"))
pft_spirometry(data)

}
\references{
Quanjer PH, Stanojevic S, Cole TJ, et al. Multi-ethnic reference values for
spirometry for the 3-95-yr age range: the global lung function 2012
equations. Eur Respir J. 2012;40(6):1324-1343.
\doi{10.1183/09031936.00080312}.

Bowerman C, Bhakta NR, Brazzale D, et al. A race-neutral approach to the
interpretation of lung function measurements. Am J Respir Crit Care Med.
2023;207(6):768-774. \doi{10.1164/rccm.202205-0963OC}.
}
\seealso{
\code{\link[=pft_volumes]{pft_volumes()}} and \code{\link[=pft_diffusion]{pft_diffusion()}} for the analogous
reference-value functions for lung volumes and diffusion capacity.
\code{\link[=pft_classify]{pft_classify()}} consumes the LLN columns produced here to assign
ATS interpretive patterns. \code{\link[=pft_interpret]{pft_interpret()}} is the one-call
wrapper that combines spirometry, volumes, diffusion, and all
downstream interpretation primitives.
}
