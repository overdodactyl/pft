% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/full_report.R
\name{pft_full_report}
\alias{pft_full_report}
\title{One-shot validate -> interpret -> plot -> report pipeline}
\usage{
pft_full_report(
  data,
  output_file = NULL,
  year = 2012,
  SI.units = FALSE,
  standard = c("2022", "2005"),
  drop_invalid = FALSE,
  plot = TRUE,
  ...
)
}
\arguments{
\item{data}{A data frame with the standard \code{pft} input contract
(see \code{\link[=pft_required_columns]{pft_required_columns()}}).}

\item{output_file}{Path for the rendered report. \code{NULL} (default)
writes to a tempfile in the session temp directory.}

\item{year, SI.units, standard}{Passed through to \code{\link[=pft_interpret]{pft_interpret()}}.}

\item{drop_invalid}{Logical. When \code{TRUE}, rows that fail
\code{\link[=pft_validate]{pft_validate()}} are excluded before interpretation. When \code{FALSE}
(default), invalid rows still flow through interpretation -- the
\code{qc_pass} / \code{qc_issues} columns are preserved so the report can
show them.}

\item{plot}{Logical. When \code{TRUE} (default), produces a \code{pft_plot()}
alongside the report and stores it in the returned list. When
\code{FALSE}, skips plot generation.}

\item{...}{Additional arguments passed to \code{\link[=pft_report]{pft_report()}} (e.g.
\code{output_format = "pdf_document"}).}
}
\value{
Invisibly, a list with components:
\itemize{
\item \code{result}: the \code{\link[=pft_interpret]{pft_interpret()}} output (with \code{qc_pass} / \code{qc_issues}
columns from \code{\link[=pft_validate]{pft_validate()}} still attached).
\item \code{report}: path to the rendered report file.
\item \code{plot}: the ggplot object from \code{\link[=pft_plot]{pft_plot()}}, or \code{NULL} if \code{plot = FALSE}.
}
}
\description{
\code{pft_full_report()} chains the four most common stages of a typical
clinical-audit workflow into a single call: input QC via
\code{\link[=pft_validate]{pft_validate()}}, pattern interpretation via \code{\link[=pft_interpret]{pft_interpret()}},
optional plot generation via \code{\link[=pft_plot]{pft_plot()}}, and rendered output via
\code{\link[=pft_report]{pft_report()}}. It removes the 4-5 lines of boilerplate that every
downstream caller would otherwise write.
}
\details{
The function is a thin wrapper -- each individual stage is exported
for callers who need finer-grained control. By default
\code{pft_full_report()} honours \code{\link[=pft_validate]{pft_validate()}} (rows that fail QC are
annotated but still interpreted, so the report shows them flagged);
set \code{drop_invalid = TRUE} to instead exclude failing rows before
interpretation.
}
\examples{
\dontrun{
cohort <- data.frame(
  sex    = c("M", "F"),
  age    = c(45,  60),
  height = c(178, 165),
  race   = c("Caucasian", "AfrAm"),
  fev1_measured = c(3.2, 1.8),
  fvc_measured  = c(4.5, 2.4),
  tlc_measured  = c(7.0, 4.8)
)
out <- pft_full_report(cohort, output_file = "cohort_report.html")
out$report  # path to the HTML file
out$plot    # ggplot object
}

}
\seealso{
\code{\link[=pft_validate]{pft_validate()}}, \code{\link[=pft_interpret]{pft_interpret()}}, \code{\link[=pft_plot]{pft_plot()}},
\code{\link[=pft_report]{pft_report()}} -- the four primitives this wraps.
}
