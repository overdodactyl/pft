% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pattern_severity.R
\name{pft_pattern_severity}
\alias{pft_pattern_severity}
\title{Compose an ATS pattern and per-measure severity into a single label}
\usage{
pft_pattern_severity(data)
}
\arguments{
\item{data}{A data frame with the columns \code{ats_classification},
\code{fev1_severity}, and \code{fvc_severity} (typically a
\code{\link[=pft_interpret]{pft_interpret()}} result). The function also accepts the 2022-
suffixed columns (\code{fev1_severity_2022}, \code{fvc_severity_2022})
when the GLI Global 2022 standard is in use, and falls back to
them if the unsuffixed columns are absent.}
}
\value{
The original data frame with a \code{pattern_severity} column
appended.
}
\description{
Stanojevic 2022 reports pattern (Normal / Non-specific / Obstructed
/ Restricted / Mixed) and per-measure severity (normal / mild /
moderate / severe) on separate axes. Clinicians often want a single
composite label like \code{"Moderate Obstructed"} or \code{"Severe Mixed"}
for cohort reporting and clinic notes. This function composes the
two using the convention that the severity reflects the worst
measure that drives the pattern:
}
\details{
\itemize{
\item \code{Obstructed} -> FEV1 severity (FEV1/FVC < LLN is the trigger;
FEV1 z-score drives the severity grade).
\item \code{Restricted} -> FVC severity (FVC < LLN with TLC < LLN is the
trigger; FVC z-score drives severity grade).
\item \code{Mixed}      -> worse of FEV1 and FVC severity.
\item \code{Non-specific} / \code{PRISm} -> FEV1 severity.
\item \code{Normal}     -> \code{"Normal"} (no severity qualifier).
\item Any NA inputs -> \code{NA}.
}

Operates on the existing wide-form output of \code{\link[=pft_interpret]{pft_interpret()}} (the
\code{ats_classification}, \code{fev1_severity}, \code{fvc_severity} columns it
already produces); no new input data is required.
}
\examples{
d <- data.frame(
  ats_classification = c("Obstructed", "Restricted", "Mixed",
                           "Non-specific", "Normal", NA),
  fev1_severity      = c("moderate", "normal", "severe",
                           "mild", "normal", "moderate"),
  fvc_severity       = c("normal", "moderate", "moderate",
                           "normal", "normal", "mild")
)
pft_pattern_severity(d)

}
\references{
Stanojevic S, Kaminsky DA, Miller MR, et al. ERS/ATS technical
standard on interpretive strategies for routine lung function
tests. \emph{Eur Respir J}. 2022;60:2101499.
\doi{10.1183/13993003.01499-2021}. The composition rule
implemented here -- severity from the driving measure -- follows
the practical reporting convention used in the standard's worked
examples (e.g., Section "Reporting and interpretation").
}
\seealso{
\code{\link[=pft_classify]{pft_classify()}} for the pattern axis; \code{\link[=pft_severity]{pft_severity()}}
for the per-measure z-score severity grader; \code{\link[=pft_interpret]{pft_interpret()}}
for the one-call workflow that produces both.
}
