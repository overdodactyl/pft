% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diffusion_capacity.R
\name{pft_diffusion}
\alias{pft_diffusion}
\title{Compute carbon monoxide diffusion capacity or transfer factor reference values for given demographics}
\usage{
pft_diffusion(data, SI.units = FALSE, sex = sex, age = age, height = height)
}
\arguments{
\item{data}{A data frame containing columns for sex ("M","F"),
age (in years, in the range 5-90) and height (in centimeters).
If \code{data} also contains a \verb{<measure>_measured} column for any of
the active measures (\code{tlco}, \code{kco_si}, \code{va} under SI units;
\code{dlco}, \code{kco_tr}, \code{va} under traditional), the measured value is
used to compute z-score and percent-predicted (see Value).}

\item{SI.units}{A boolean. Returns the reference values in SI units if TRUE
.  and Traditional units if FALSE.}

\item{sex, age, height}{Column references. By default \code{pft_diffusion()}
reads from \code{sex}, \code{age}, and \code{height}. Override via a bare name
(\code{sex = Sex}), a string (\code{sex = "Sex"}), or an rlang injection
(\code{sex = !!my_var}). The user's original column names are preserved
in the output. See \code{\link[=pft_required_columns]{pft_required_columns()}} for the full input
contract.}
}
\value{
The original data frame with extra columns appended for each
measure:
\itemize{
\item \verb{<measure>_pred}: predicted (median) value.
\item \verb{<measure>_lln}:  lower limit of normal (5th percentile).
\item \verb{<measure>_uln}:  upper limit of normal (95th percentile).
If a \verb{<measure>_measured} column was supplied in \code{data}, two
additional columns are emitted:
\item \verb{<measure>_zscore}:  LMS z-score \code{((measured/M)^L - 1) / (L*S)}.
\item \verb{<measure>_pctpred}: percent predicted \code{(measured / pred) * 100}.
}
}
\description{
\code{pft_diffusion()} computes ATS-compliant upper and lower normal limits
for carbon monoxide measured diffusion capacity and European equivalents
including DLCO (or TLCO), KCO, and VA.
}
\examples{
data <- data.frame(sex=c("M","F"),
                   age=c(30,5.1),
                   height=c(178,50))
pft_diffusion(data)

}
\references{
Stanojevic S, Graham BL, Cooper BG, et al. Official ERS technical standards:
Global Lung Function Initiative reference values for the carbon monoxide
transfer factor for Caucasians. Eur Respir J. 2017;50(3):1700010.
\doi{10.1183/13993003.00010-2017}. (Author correction:
\doi{10.1183/13993003.50010-2017}, applied here.)
}
\seealso{
\code{\link[=pft_spirometry]{pft_spirometry()}} and \code{\link[=pft_volumes]{pft_volumes()}} for the analogous
reference-value functions. \code{\link[=pft_severity]{pft_severity()}} grades DLCO impairment
severity from the z-score column produced here. \code{\link[=pft_interpret]{pft_interpret()}}
composes all three reference functions in one call.
}
