% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guidance.R
\name{pft_clinical_guidance}
\alias{pft_clinical_guidance}
\title{Clinical-style guidance text from an interpreted PFT result}
\usage{
pft_clinical_guidance(data)
}
\arguments{
\item{data}{A data frame produced by \code{\link[=pft_interpret]{pft_interpret()}}.}
}
\value{
The original data frame with one new character column
appended:
\itemize{
\item \code{guidance}: a short narrative string per row. Empty string \code{""}
when the inputs needed to produce guidance aren't present; \code{NA}
propagates from missing pattern labels.
}
}
\description{
Maps the pattern + severity columns produced by \code{\link[=pft_interpret]{pft_interpret()}} to
short, plain-language guidance strings that summarise the
physiological finding and suggest a typical next step. Useful for
report templates, clinic notes, and patient handouts where the raw
classification label and z-score don't communicate "what to do
about it".
}
\details{
The text is deliberately conservative -- it describes the result and
the kind of work-up the ERS/ATS 2022 standard suggests, but does not
itself constitute a diagnosis. It is built entirely from columns the
package already emits (\code{ats_classification}, \code{fev1_severity},
\code{fvc_severity}, \code{prism}, \code{volume_subpattern}, \verb{<measure>_bdr_significant})
and adds no new clinical content beyond the published standard.
}
\examples{
patient <- data.frame(
  sex = "M", age = 45, height = 178, race = "Caucasian",
  fev1_measured = 2.5, fvc_measured = 3.8,
  fev1fvc_measured = 2.5 / 3.8, tlc_measured = 6.0
)
result <- pft_interpret(patient)
pft_clinical_guidance(result)$guidance

}
\seealso{
\code{\link[=pft_interpret]{pft_interpret()}} to produce the input data frame;
\code{\link[=pft_pattern_severity]{pft_pattern_severity()}} for the composite pattern + severity
label this guidance is built on top of.
}
