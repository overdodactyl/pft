% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pft_result.R
\name{pft_glance}
\alias{pft_glance}
\title{Per-patient summary of a \code{pft_result}}
\usage{
pft_glance(x, ...)
}
\arguments{
\item{x}{A data frame; typically a \code{pft_result} from \code{\link[=pft_interpret]{pft_interpret()}}.}

\item{...}{Currently unused; reserved for forward compatibility.}
}
\value{
A tibble with one row per row of \code{x}. Always contains
\code{.patient} (row position); other columns are added only when
present in \code{x}.
}
\description{
Returns one row per patient with the highest-level interpretation
columns (\code{ats_classification}, \code{ats_pattern_combination}, \code{prism},
\code{volume_subpattern}) when present, plus three derived per-patient
summary statistics computed across all z-score columns in \code{x}:
\code{worst_zscore} (the z-score with greatest absolute value),
\code{n_below_lln} (count of z-scores below -1.645), and \code{n_above_uln}
(count above +1.645). This is the natural shape for \code{broom}-style
"one row of metadata per fit" workflows and for cohort-level joins.
}
\examples{
cohort <- data.frame(
  sex = c("M","F","M"), age = c(45,60,30), height = c(178,165,175),
  race = "Caucasian",
  fev1_measured = c(2.5, 1.8, 4.0),
  fvc_measured  = c(3.8, 2.4, 5.2),
  fev1fvc_measured = c(0.66, 0.75, 0.77),
  tlc_measured  = c(6.0, 4.5, 6.8)
)
pft_glance(pft_interpret(cohort))

}
\seealso{
\code{\link[=pft_long]{pft_long()}} for the per-measure long-form pivot.
}
