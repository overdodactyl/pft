% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pft_result.R
\name{pft_long}
\alias{pft_long}
\title{Pivot a \code{pft_result} to long form}
\usage{
pft_long(x, ...)
}
\arguments{
\item{x}{A data frame; typically a \code{pft_result} but any data frame
with \verb{<measure>_pred[_<year>]} columns works. Named \code{x} (rather
than \code{data}) to match the S3 first-argument convention shared with
\code{print.pft_result}, \code{plot.pft_result}, and the other \code{pft_result}
methods.}

\item{...}{Currently unused; reserved for forward compatibility.}
}
\value{
A tibble with columns \code{.patient} (integer row position),
\code{measure}, \code{year} (character; \code{NA} for non-suffixed outputs),
\code{pred}, \code{lln}, \code{uln}, \code{measured}, \code{zscore}, \code{pctpred}, and
\code{severity}. Missing statistics fill with \code{NA} of the appropriate
type.
}
\description{
Reshapes a wide \code{\link[=pft_interpret]{pft_interpret()}} / \code{\link[=pft_spirometry]{pft_spirometry()}} / \code{\link[=pft_volumes]{pft_volumes()}} /
\code{\link[=pft_diffusion]{pft_diffusion()}} output (one row per patient, one column per measure ×
statistic) into long form (one row per \verb{(patient, measure, year)} with
columns for each statistic). This is the natural shape for \code{dplyr} /
\code{ggplot2} faceting, cohort modelling, and \code{broom}-style downstream
workflows.
}
\details{
Discovery is keyed off \verb{<measure>_pred} columns; the four-digit GLI
year is extracted from the column suffix and recorded in the \code{year}
column. Spirometry outputs from \code{\link[=pft_spirometry]{pft_spirometry()}} / \code{\link[=pft_interpret]{pft_interpret()}}
always carry a year suffix (\code{fev1_pred_2012}, \code{fev1_pred_2022}, ...)
and produce a populated \code{year}; lung-volume (Hall 2021) and
diffusion (GLI 2017) outputs are unsuffixed and produce \code{year = NA}
until a competing standard ships and the same suffixing convention
is adopted there. Columns whose suffix does not match a recognised
statistic are ignored, so id / demographic columns are dropped (use
the \code{.patient} integer to join back).
}
\examples{
patient <- data.frame(
  sex = c("M","F"), age = c(45, 60), height = c(178, 165),
  race = "Caucasian",
  fev1_measured = c(2.5, 1.8), fvc_measured = c(3.8, 2.4)
)
result <- pft_interpret(patient)
pft_long(result)

}
\seealso{
\code{\link[=pft_interpret]{pft_interpret()}}
to produce the wide-form input.
}
