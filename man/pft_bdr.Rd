% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpretation.R
\name{pft_bdr}
\alias{pft_bdr}
\title{Bronchodilator response per the current ATS/ERS criterion}
\usage{
pft_bdr(pre, post, predicted, threshold = BDR_THRESHOLD_PCT_PRED)
}
\arguments{
\item{pre, post}{Numeric vectors of pre- and post-bronchodilator
measurements (same units, same length).}

\item{predicted}{Numeric vector of predicted (median) values for the
same measure, typically the \verb{<measure>_pred} column from a previous
call to \code{pft_spirometry()}.}

\item{threshold}{Percent-of-predicted change considered significant.
Defaults to 10 (the Stanojevic 2022 criterion).}
}
\value{
A data frame with one row per input observation and columns:
\itemize{
\item \code{pct_pred_change}: \code{(post - pre) / predicted * 100}.
\item \code{is_significant}: logical, \code{TRUE} when \code{pct_pred_change > threshold}.
\code{NA} is propagated wherever any of \code{pre}, \code{post}, \code{predicted} is \code{NA}.
}
}
\description{
Classifies bronchodilator response (BDR) by the percent change in
the measured value relative to the patient's predicted value, as
recommended by Stanojevic et al. ERJ 2022. Significant BDR is defined
as a post-bronchodilator increase of more than 10\% of the predicted
value in either FEV1 or FVC. This replaces the 2005 standard, which
used a >=12\% AND >=200 mL change from baseline.
}
\section{Column naming}{

This function's \code{pct_pred_change} column is \strong{percent-of-predicted}
change (the 2022 criterion). The predecessor \code{\link[=pft_bdr_2005]{pft_bdr_2005()}} emits a
similarly-named but different column, \code{pct_change}, which is
\strong{percent-of-baseline} change (\code{(post - pre) / pre * 100}, the 2005
criterion). The two functions deliberately use distinct column names
so a result frame can carry both without ambiguity.
}

\examples{
pft_bdr(pre = 2.5, post = 3.0, predicted = 4.0)
# -> 12.5\% of predicted change, is_significant = TRUE

}
\references{
Stanojevic S, Kaminsky DA, Miller MR, et al. ERS/ATS technical standard
on interpretive strategies for routine lung function tests. Eur Respir J.
2022;60(1):2101499. \doi{10.1183/13993003.01499-2021}. See the
"Bronchodilator responsiveness testing" section.
}
\seealso{
\code{\link[=pft_spirometry]{pft_spirometry()}} to obtain the predicted FEV1 / FVC values
used as the denominator. \code{\link[=pft_interpret]{pft_interpret()}} runs BDR automatically
when \verb{<measure>_pre} and \verb{<measure>_post} columns are present.
}
