% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{pft_compare}
\alias{pft_compare}
\title{Compare GLI 2012 (race-stratified) vs GLI Global 2022 (race-neutral) interpretation}
\usage{
pft_compare(
  data,
  SI.units = FALSE,
  standard = c("2022", "2005"),
  sex = sex,
  age = age,
  height = height,
  race = race
)
}
\arguments{
\item{data}{A data frame with the standard \code{pft} input contract:
\code{sex}, \code{age}, \code{height}, \code{race}, plus any \verb{<measure>_measured} or
\verb{<measure>_pre} / \verb{_post} columns desired. A \code{race} column is
\emph{required} (the GLI 2012 path uses it; the GLI Global 2022 path
ignores it).}

\item{SI.units}{Diffusion unit system (passed to \code{\link[=pft_interpret]{pft_interpret()}}).}

\item{standard}{Interpretive standard to apply on top of the
reference values. \code{"2022"} (default; Stanojevic) uses z-score-based
severity; \code{"2005"} (Pellegrino) uses percent-predicted-based
severity and an older pattern-classification flowchart. The same
interpretive standard is applied to both equation sets so the
comparison isolates the equation effect.}

\item{sex, age, height, race}{Column references. See \code{\link[=pft_interpret]{pft_interpret()}}.}
}
\value{
A tibble with class \code{pft_compare} (also inherits from
\code{tbl_df}). Contains:
\itemize{
\item All standard GLI 2012 reference / z-score / severity / pattern /
PRISm / BDR / lung-volume sub-pattern columns (unsuffixed names).
\item The corresponding GLI Global 2022 columns with a \verb{_2022} suffix
(\code{fev1_zscore_2022}, \code{ats_classification_2022},
\code{volume_subpattern_2022}, etc.).
\item Shared GLI 2021 lung-volume and GLI 2017 diffusion columns (no
suffix; identical between standards).
\item Per-row deltas / reclassification flags:
\itemize{
\item \verb{<measure>_zscore_delta} (numeric; 2022 minus 2012)
\item \verb{<measure>_severity_changed} (logical)
\item \code{ats_pattern_changed} (logical) and \code{ats_pattern_change}
(character; e.g. \code{"Normal -> Obstructed"} or \code{""} when unchanged)
\item \code{prism_changed}, \code{volume_subpattern_changed} (logical)
}
}
}
\description{
\code{pft_compare()} runs the spirometry + interpretation pipeline twice
on the same input data -- once under the GLI 2012 race-stratified
equations (Quanjer 2012) and once under the GLI Global 2022
race-neutral equations (Bowerman 2023) -- and emits a single tibble
with both sets of outputs and per-row reclassification deltas. Use
it when migrating cohorts from GLI 2012 to GLI Global 2022 and you
need to report how labels shifted.

Lung volumes (GLI 2021) and diffusion (GLI 2017) are \emph{not}
year-stratified and are computed once. Only spirometry-derived
columns (z-scores, severities, BDR, classification, PRISm, and
lung-volume sub-pattern -- the latter two depend on FEV1/FVC LLN) get
a \verb{_2022} companion column.
}
\section{Reclassification semantics}{

A \verb{_changed} flag is \code{TRUE} if the 2012 and 2022 labels differ;
\code{FALSE} if they agree; \code{NA} if either label is \code{NA} (e.g., one of
the underlying inputs was missing). This matches the convention
used by \code{pft_change()} and the cohort-summary helpers, and means
downstream filters like \code{sum(ats_pattern_changed, na.rm = TRUE) / sum(!is.na(ats_pattern_changed))} give an interpretable
reclassification rate.
}

\examples{
patient <- data.frame(
  sex = c("M", "F"), age = c(45, 60), height = c(178, 165),
  race = c("AfrAm", "Caucasian"),
  fev1_measured    = c(2.5, 1.8),
  fvc_measured     = c(3.8, 2.4),
  fev1fvc_measured = c(0.66, 0.75),
  tlc_measured     = c(6.0, 4.5)
)
cmp <- pft_compare(patient)
summary(cmp)

}
\references{
Quanjer PH, Stanojevic S, Cole TJ, et al. (GLI 2012)
\doi{10.1183/09031936.00080312}.

Bowerman C, Bhakta NR, Brazzale D, et al. A race-neutral approach to
the interpretation of lung function measurements. Am J Respir Crit
Care Med. 2023;207(6):768-774. \doi{10.1164/rccm.202205-0963OC}.
}
\seealso{
\code{\link[=pft_interpret]{pft_interpret()}} for the single-standard workflow;
\code{\link[=pft_cohort_summary]{pft_cohort_summary()}} for cohort-level reclassification audits;
\code{\link[=summary.pft_compare]{summary.pft_compare()}} for the print-friendly reclassification
summary.
}
