% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lung_age.R
\name{pft_lung_age}
\alias{pft_lung_age}
\title{Compute lung age from measured spirometry}
\usage{
pft_lung_age(
  data,
  measure = c("fev1", "fvc"),
  year = 2012,
  age_range = c(3, 95),
  sex = sex,
  age = age,
  height = height,
  race = race
)
}
\arguments{
\item{data}{A data frame with the standard \code{pft} demographics
(\code{sex}, \code{age}, \code{height}, and for \code{year = 2012} a \code{race} column)
plus at least one of \code{fev1_measured}, \code{fvc_measured}. The
patient's \code{age} column is read for diagnostic purposes (it is
not used in the inversion, but allows the output to also report
the chronological vs lung-age delta).}

\item{measure}{Which measure to invert. \code{"fev1"} (default) or
\code{"fvc"}. Other measures (FEF, FEV1/FVC) are not monotonic in age
over the full GLI range and are not supported.}

\item{year}{GLI equation year. \code{2012} (default, race-stratified)
or \code{2022} (race-neutral).}

\item{age_range}{Numeric length-2 search interval for the
inversion. Default \code{c(3, 95)} matches the GLI 2012 / GLI Global
2022 supported range; tighten to \code{c(18, 95)} for adult-only
workflows.}

\item{sex, age, height, race}{Column references. See \code{\link[=pft_spirometry]{pft_spirometry()}}.}
}
\value{
The original data frame with extra columns:
\itemize{
\item \verb{<measure>_lung_age}: lung age in years. \code{NA} if the measured
value is missing, if uniroot fails to bracket a root (i.e. the
measured value is outside the predicted range across \code{age_range}
for that height / sex / race), or if any required demographic is
missing.
\item \verb{<measure>_lung_age_delta}: \verb{<measure>_lung_age - <age column>}.
Positive values mean the patient's lungs perform like an older
person's; negative means younger.
}
}
\description{
"Lung age" is the age at which the patient's measured FEV1 (or FVC)
would equal the GLI predicted value for someone of their height,
sex, and (under GLI 2012) race. It is the algebraic inverse of the
GLI predicted formula evaluated at the patient's actual measurement,
and is widely used as a patient-communication tool in smoking
cessation and asthma counseling ("your lungs are equivalent to a
65-year-old's").
}
\details{
Implementation is a numeric inversion of \code{\link[=pft_spirometry]{pft_spirometry()}} via
\code{\link[stats:uniroot]{stats::uniroot()}} over the age range supported by the GLI
equations. No new reference data is involved -- the same GLI 2012
or GLI Global 2022 splines and coefficients that produce
\verb{<measure>_pred} are walked in reverse.
}
\section{Limitations}{

Lung age is a heuristic, not a diagnostic measure. It compresses
the LMS distribution into a single age scalar and discards the
variability information (S, L). For clinical interpretation use
the z-score from \code{\link[=pft_spirometry]{pft_spirometry()}}; reserve lung age for patient
counseling and risk-communication contexts.
}

\examples{
patient <- data.frame(
  sex = c("M", "F"), age = c(45, 60), height = c(178, 165),
  race = "Caucasian",
  fev1_measured = c(2.5, 1.8)
)
pft_lung_age(patient)

}
\references{
Morris JF, Temple W. Spirometric "lung age" estimation for motivating
smoking cessation. Prev Med. 1985;14(5):655-662.
\doi{10.1016/0091-7435(85)90085-4}. (Original concept; closed-form
regression. The GLI-based inversion implemented here is the modern
continuous-age extension.)
}
\seealso{
\code{\link[=pft_spirometry]{pft_spirometry()}} for the forward predicted-value
calculation; \code{\link[=pft_fev1q]{pft_fev1q()}} for the complementary FEV1Q
adult-mortality index.
}
